<% if climber_signed_in? %>
		<%= link_to "Log Out", destroy_climber_session_path, method: 'delete' %>
		<%= current_climber.email %>
	<% else %>
		<%= link_to "Log In", new_climber_session_path %>
		<%= link_to "Sign Up", new_climber_registration_path %>
<% end %>